# Global AI Agent Configuration
agent_id: lawyers_hub_orchestrator
version: "1.0.0"

settings:
  default_autonomy_level: "suggestive"
  max_recursion_depth: 3
  enable_audit_log: true

thresholds:
  risk_score_escalation: 0.7
  confidence_score_min: 0.6
